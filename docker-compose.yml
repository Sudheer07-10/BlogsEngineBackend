services:
  backend:
    build: .
    container_name: vertical-pulse-backend
    ports:
      - "8060:8060"
    env_file:
      - .env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
